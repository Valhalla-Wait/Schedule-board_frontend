import React from 'react';
import styled from 'styled-components';

const MainPage = () => <Wrapper>MainPage</Wrapper>;

const Wrapper = styled.div`
  background-color: aqua;
`;

export default MainPage;
